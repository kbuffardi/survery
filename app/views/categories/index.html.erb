
<div class="progress">
  <div class="progress-bar progress-bar-info progress-bar-striped" role="progressbar" aria-valuenow="0" aria-valuemin="0" aria-valuemax="100" style="min-width: 1.5em;">
<span class="progress-text">0% Complete</span>
  </div>
</div>

<div class="vert-space-sm"> </div>

<center><h3 style="font-size: 2em">Please read through the following food categories and foods</h3><br></center>

<div class="vert-space-lg"></div>

<div class="col-md-3">
  <div style="font-size: 1.3em; margin-bottom: 8px; font-weight: bold"><%= Category.find(4).name %></div>
</div>
<div class="col-md-3">
  <div style="font-size: 1.3em; margin-bottom: 8px; font-weight: bold"><%= Category.find(9).name %></div>
</div>
<div class="col-md-3">
  <div style="font-size: 1.3em; margin-bottom: 8px; font-weight: bold"><%= Category.find(3).name %></div>
</div>
<div class="col-md-3">
  <div style="font-size: 1.3em; margin-bottom: 8px; font-weight: bold"><%= Category.find(11).name %></div>
</div>
<div class="col-md-3">
  <div style="font-size: 1.3em; margin-bottom: 8px; font-weight: bold"><%= Category.find(1).name %></div>
</div>
<div class="col-md-3">
  <div style="font-size: 1.3em; margin-bottom: 8px; font-weight: bold"><%= Category.find(5).name %></div>
</div>
<div class="col-md-3">
  <div style="font-size: 1.3em; margin-bottom: 8px; font-weight: bold"><%= Category.find(6).name %></div>
</div>
<div class="col-md-3">
  <div style="font-size: 1.3em; margin-bottom: 8px; font-weight: bold"><%= Category.find(8).name %></div>
</div>
<div class="col-md-3">
  <div style="font-size: 1.3em; margin-bottom: 8px; font-weight: bold"><%= Category.find(12).name %></div>
</div>
<div class="col-md-3">
  <div style="font-size: 1.3em; margin-bottom: 8px; font-weight: bold"><%= Category.find(10).name %></div>
</div>
<div class="col-md-3">
  <div style="font-size: 1.3em; margin-bottom: 8px; font-weight: bold"><%= Category.find(7).name %></div>
</div>
<div class="col-md-3">
  <div style="font-size: 1.3em; margin-bottom: 8px; font-weight: bold"><%= Category.find(2).name %></div>
</div>

<div class="vert-space-lg"></div>

<div class="col-md-7 col-md-offset-1">
  <h4><strong><%= Category.find(4).name %></strong></h4>

  <% Category.find(4).foods.each do |food| %>
  <div class="col-md-offset-1">
    <%= food.name %>
  </div>
  <% end %>
</div>

<div class="col-md-4">
  <h4><strong><%= Category.find(9).name %></strong></h4>

  <% Category.find(9).foods.each do |food| %>
  <div class="col-md-offset-2" >
    <%= food.name %>
  </div>
  <% end %>
</div>

<div class="vert-space-sm"></div>
<div class="col-md-7 col-md-offset-1">
  <h4><strong><%= Category.find(3).name %></strong></h4>

  <% Category.find(3).foods.each do |food| %>
  <div class="col-md-offset-1" >
    <%= food.name %>
  </div>
  <% end %>
</div>

<div class="col-md-4">
  <h4><strong><%= Category.find(11).name %></strong></h4>

  <% Category.find(11).foods.each do |food| %>
  <div class="col-md-offset-2" >
    <%= food.name %>
  </div>
  <% end %>
</div>

<div class="vert-space-sm"></div>
<div class="col-md-7 col-md-offset-1">
  <h4><strong><%= Category.find(1).name %></strong></h4>

  <% Category.find(1).foods.each do |food| %>
  <div class="col-md-offset-1" >
    <%= food.name %>
  </div>
  <% end %>
</div>

<div class="col-md-4">
  <h4><strong><%= Category.find(5).name %></strong></h4>

  <% Category.find(5).foods.each do |food| %>
  <div class="col-md-offset-2" >
    <%= food.name %>
  </div>
  <% end %>
</div>

<div class="vert-space-sm"></div>

<div class="col-md-7 col-md-offset-1">
  <h4><strong><%= Category.find(6).name %></strong></h4>
  <% Category.find(6).foods.each do |food| %>
  <div class="col-md-offset-1" >
    <%= food.name %>
  </div>
  <% end %>
</div>

<div class="col-md-4">
  <h4><strong><%= Category.find(8).name %></strong></h4>
  <% Category.find(8).foods.each do |food| %>
  <div class="col-md-offset-2" >
    <%= food.name %>
  </div>
  <% end %>
</div>

<div class="vert-space-sm"></div>

<div class="col-md-7 col-md-offset-1">
  <h4><strong><%= Category.find(12).name %></strong></h4>
  <% Category.find(12).foods.each do |food| %>
  <div class="col-md-offset-1" >
    <%= food.name %>
  </div>
  <% end %>
</div>

<div class="col-md-4">
  <h4><strong><%= Category.find(10).name %></strong></h4>
  <% Category.find(10).foods.each do |food| %>
  <div class="col-md-offset-2" >
    <%= food.name %>
  </div>
  <% end %>
</div>

<div class="vert-space-sm"></div>

<div class="col-md-7 col-md-offset-1">
  <h4><strong><%= Category.find(7).name %></strong></h4>
  <% Category.find(7).foods.each do |food| %>
  <div class="col-md-offset-1" >
    <%= food.name %>
  </div>
  <% end %>
</div>

<div class="col-md-4">
  <h4><strong><%= Category.find(2).name %></strong></h4>
  <% Category.find(2).foods.each do |food| %>
  <div class="col-md-offset-2" >
    <%= food.name %>
  </div>
  <% end %>
</div>
<div class="vert-space-sm"></div>


<%= button_to "Start Survey", survey_category_selection_path, class: "col-md-offset-9 btn btn-primary btn-surv", method: :get %>


<!-- FOR DYNAMIC PAGE

<div class="vert-space-lg"></div>

<% @categories.each do |category| %>
    <div class="col-md-3"  style="">
    <div style="font-size: 1.2em; margin-bottom: 8px; font-weight: bold"><%= category.name %></div>
</div>
<% end %>

<div class="vert-space-lg"></div>

<% @categories.each_with_index do |category, i| %>
    <div class="col-md-6" style=" <% if i == 6 || i == 7 %>
                                     min-height: 290px;
                                  <% elsif i == 5 || i == 4 %>
                                    min-height: 165px;
                                  <% else %>
                                    min-height: 215px;
                                  <% end %> ">
    <h4><strong><%= category.name %></strong></h4>


    <% category.foods.each do |food| %>
    <div class="col-md-offset-1" >
      <%= food.name %>
    </div>
    <% end %>
    </div>
<% end %>

-->

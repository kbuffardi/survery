
<div class="progress">
  <div class="progress-bar progress-bar-info progress-bar-striped" role="progressbar" aria-valuenow="5" aria-valuemin="0" aria-valuemax="100" style="width: 5%">
    <span class="progress-text">PROGRESS&nbsp;BAR</span>
  </div>
</div>

<h3>Which of these food Categories do you feel are problematic for you? &nbsp;<span style="font-size: 14px; font-style: italic;"> (mark at least 1)</span></h3>

<hr>




<div class="selections col-md-offset-1 checkbox checkbox-primary" style="font-size: 1.1em">
<%= form_for @survey do |f| %>
	<%= f.hidden_field :user_id, :value => current_user.id %>
	<%= f.collection_check_boxes :category_ids, Category.all, :id, :name, {:item_wrapper_class => 'checkbox'}, :multiple => true %><br>
</div>

<hr>
	<%= f.submit 'Next', :class => "col-md-offset-8 btn btn-primary btn-surv" %>
<% end %>
</div>

<script>
$( document ).ready(function() {
  $( "#e1" ).select2();
});
</script>

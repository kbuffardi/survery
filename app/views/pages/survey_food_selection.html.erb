<div class="progress">
  <div class="progress-bar progress-bar-info progress-bar-striped" role="progressbar" aria-valuenow="10" aria-valuemin="0" aria-valuemax="100" style="width: 10%">
    <span class="progress-text">10% Complete</span>
  </div>
</div>

<h3>Which of these foods do you feel are problematic for you? &nbsp;<span style="font-size: 14px; font-style: italic">(mark at least 1)</span></h3>

<hr>

<div class="selections col-md-offset-1 checkbox checkbox-primary">
<%= form_for @survey do |f| %>
	<% @categories.each do |category| %>
		<h4 style="margin-bottom: 15px; margin-left: -5px"><strong><%= category.name %></strong></h4>
<div style="margin-left: 16px">
		<%= f.collection_check_boxes :food_ids, category.foods.all, :id, :name, {}, :multiple => true %><br>
    </div>
	<% end %>
	<%= f.submit 'Next', :class => "col-md-offset-8 btn btn-primary btn-surv" %>

<% end %>
</div>
